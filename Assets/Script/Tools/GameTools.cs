using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEditor;


public class GameTools : MonoBehaviour
{
  /*  public static string path = "Assets/Scenes/Wellcom.unity";
    public static string curScentPath;
    [MenuItem("GameTools/Launcher %F1")]
    public static void LauncherGame()
    {
        if (EditorUtility.DisplayDialog("是否开始" + path, "场景没有保存的话内容会丢失哦", "开始~", "还是算了") == false)
        {

            return;
        }
        EditorApplication.isPaused = false;
        EditorApplication.isPlaying = false;
        EditorSceneManager.OpenScene(path);
        EditorApplication.isPlaying = true;
    }*/
}
